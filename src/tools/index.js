
  // .filter(cont=> cont.name.search('')>=0 )
  // .sort(
  //   (a, b)=>{
  //   if(a.name < b.name) { return -1; }
  //   if(a.name > b.name) { return 1; }
  //   return 0}
  //   )
  // const regex = new RegExp((`\\b${inputValue}`, 'gi')) 